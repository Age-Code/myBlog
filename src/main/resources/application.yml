spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.myblog.dto

server:
  port: 8080
